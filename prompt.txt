Reference CLAUDE.md and PLAN.md for full context before doing anything.

Task: Finish this phase of src/preflight/report.py. Implement
Report.to_dict() and Report.to_dataframe(). Sub-step 3 of 3.

Implement:

    def to_dict(self) -> dict
        — returns a JSON-serializable dict representation:
          {
            "summary": self.summary_counts(),
            "total_entries": len(self.entries),
            "entries": [
                {"stage": ..., "column": ..., "action": ..., "rationale": ...,
                 "severity": ..., "before_stats": ..., "after_stats": ...},
                ...
            ]
          }
          Must be genuinely JSON-serializable — before_stats/after_stats
          dicts may contain numpy types (e.g. np.float64) from upstream
          profiler/cleaner computations; convert these to native Python
          float/int so json.dumps() doesn't choke. This matters directly
          for cli.py's planned <name>_report.json output in Phase 8.

    def to_dataframe(self) -> pd.DataFrame
        — returns a pandas DataFrame with one row per ReportEntry, columns:
          stage, column, action, rationale, severity, before_stats,
          after_stats (the stats dicts can remain as dict objects in their
          cells — no need to flatten for this method, unlike to_dict).
          Empty report returns an empty DataFrame with the correct columns
          (not a DataFrame with zero columns).

Type hints per convention.

After implementing, update CLAUDE.md's "Current focus": mark this phase of
report.py complete under "Recent completions" — note that .plot() and
.to_html() remain as a separate later phase per PLAN.md's implementation
order (step 12, after assembler.py). Update PLAN.md's implementation order
checklist (step 9 done). Set "Active work" to assembler.py (Phase 6) as
next, since that's the next step in PLAN.md's order before report visuals.  also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 







 also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 