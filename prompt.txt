Reference CLAUDE.md and PLAN.md for full context before doing anything.

Task: Continue src/preflight/cli.py. Implement output file writing per
PLAN.md's documented CLI outputs: "<name>_prepared.csv, <name>_pipeline.joblib,
<name>_report.json". Sub-step 2 of 3.

Implement:

    def write_outputs(
        result: PrepResult,
        input_path: str,
        output_dir: str,
    ) -> dict[str, str]
        — derives <name> from input_path's filename stem (e.g.
          "train.csv" -> "train"). Writes:
          - {output_dir}/{name}_prepared.csv — result.df via
            df.to_csv(path, index=False)
          - {output_dir}/{name}_pipeline.joblib — result.pipeline via
            joblib.dump(), per the locked stack's serialization choice.
            Skip writing this file (do not error) if result.pipeline is
            None — this keeps write_outputs reusable for profile()/clean()/
            engineer() results too, which have pipeline=None by design
            from Phase 8, in case cli.py later exposes those as separate
            subcommands.
          - {output_dir}/{name}_report.json — result.report.to_dict() (from
            Phase 7) written via json.dump(), or an empty {} skipped
            gracefully if result.report is None.
          Returns a dict mapping output type -> written file path (only
          for files actually written), e.g. {"csv": "...", "pipeline":
          "...", "report": "..."} — useful both for the prepare command's
          final echo and for testing.

Wire write_outputs() into the `prepare` command from sub-step 1: after a
successful preflight.prepare() call, call write_outputs() and typer.echo
each output path that was written, replacing sub-step 1's placeholder
"success + shape" message with a fuller one that also reports the shape.

Type hints per convention. Handle file write failures (e.g. permission
errors) by catching OSError and re-raising as a clean typer.echo + Exit(1),
consistent with sub-step 1's error-handling pattern.

After implementing, update CLAUDE.md's "Current focus": note cli.py 1 of
3 sub-steps remaining (integration hardening / test_cli.py completeness).also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 






 also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 