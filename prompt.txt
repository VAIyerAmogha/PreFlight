Reference CLAUDE.md and PLAN.md for full context before doing anything.

Task: Finish src/preflight/profiler.py by implementing the orchestration
layer that ties together the 3 prior sub-steps (type inference, structural
signals, target-dependent signals) into the module's public entry point.

Implement:

    def run_profiler(
        df: pd.DataFrame,
        target: str,
        task: str,  # "regression" | "classification"
        cardinality_threshold: int = 20,
    ) -> tuple[list[ColumnProfile], list[ReportEntry]]:

Behavior:
- For every column except `target`, build a ColumnProfile using the
  functions from steps 2.1–2.3.
- Only compute target-dependent signals (correlation, MI, leakage,
  class imbalance) where semantically valid — e.g. skip correlation for
  CATEGORICAL_HIGH columns, per each function's own None-return contract.
- Only compute rare_categories for CATEGORICAL_LOW / CATEGORICAL_HIGH types.
- Run VIF once across all NUMERIC_FEATURE columns (not per-column) since
  it requires the full numeric submatrix — call compute_vif_scores once
  and distribute results into each ColumnProfile.
- Every decision that produces a ColumnProfile field with signal (e.g.
  "flagged as leakage suspect", "VIF cap triggered", "mixed types detected
  in column X") must emit a corresponding ReportEntry with stage="profiler"
  per CLAUDE.md's "nothing silent" rule. Purely descriptive signals (e.g. a
  normal missing_rate of 0.05) do not need their own ReportEntry — only
  emit entries for warning/critical-worthy signals: high missingness,
  leakage suspicion, VIF cap triggered, mixed types detected, high
  cardinality, high imbalance ratio.
- Per CLAUDE.md: "Do not re-infer SemanticType after Profiler has run" —
  this is the only place SemanticType gets computed. Make this an explicit
  code comment marking the boundary.
- Type hints per convention. Function should not mutate input df.

After implementing, update CLAUDE.md's "Current focus": mark profiler.py
fully complete under "Recent completions", update PLAN.md's implementation
order checklist (steps 3-4 done), and set "Active work" to cleaner.py
(Phase 3) as next.  also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 







 also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 