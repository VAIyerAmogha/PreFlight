Reference CLAUDE.md and PLAN.md for full context before doing anything.

Task: Finish src/preflight/engineer.py. Implement the orchestration entry
point tying together sub-steps 1-3, with model_hint branching per PLAN.md's
locked encoding table.

Implement:

    def run_engineer(
        df: pd.DataFrame,
        profiles: list[ColumnProfile],
        target: str,
        model_hint: str,  # "tree" | "linear"
        cardinality_threshold: int = 20,
    ) -> tuple[pd.DataFrame, list[ReportEntry], dict]

Behavior:
- Raise ValueError if model_hint not in ("tree", "linear").
- For each column (by semantic_type from its ColumnProfile — do not
  re-infer):
  - DATETIME_NATIVE: expand_datetime, replace original column with the
    expanded columns.
  - CATEGORICAL_LOW: 
      - model_hint="tree" -> ordinal_encode
      - model_hint="linear" -> one_hot_encode
  - CATEGORICAL_HIGH: target_encode_cross_fit in BOTH branches (per
    PLAN.md, high-cardinality always uses target encoding regardless of
    model_hint — this isn't in the model_hint branch table, it's
    unconditional on cardinality).
  - NUMERIC_FEATURE:
      - model_hint="linear": apply needs_log_transform check -> log1p_transform
        if flagged, THEN standard_scale.
      - model_hint="tree": pass through unchanged, no scaling, no log
        transform, per PLAN.md's explicit "tree: ... no scaling, no log
        transform" rule.
  - BOOLEAN, missing-indicator columns (name ending in "_missing"): pass
    through unchanged in both branches (already 0/1-compatible per
    PLAN.md's "Missing indicator passthrough" feature).
  - CONSTANT: pass through unchanged (should be rare — Cleaner mostly
    removes these upstream, but don't crash if present).
- Emit a ReportEntry (stage="engineer") for every transform applied —
  column, action taken, rationale referencing model_hint, severity="info"
  for normal transforms. If target_encode_cross_fit is applied to a
  CATEGORICAL_HIGH column, always log this at severity="info" noting
  cross-fit was used specifically to prevent leakage, referencing the
  CLAUDE.md convention.
- Return the transformed df, ReportEntry list, and a dict of transform
  specs (ordinal mappings, one-hot column lists, target-encode mappings,
  scale params, log-transform flags) — this becomes assembler.py's raw
  material for building the actual sklearn ColumnTransformer in Phase 6.
- Target column itself is never transformed — pass through only.

Type hints per convention. Original df not mutated.

After implementing, update CLAUDE.md's "Current focus": mark engineer.py
fully complete under "Recent completions", update PLAN.md's implementation
order checklist (steps 7-8 done), set "Active work" to report.py
(Phase 5) as next.also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 







 also update the plan.md with relavant entries under "Architecture decisions log" and "Implementation order" sections. 